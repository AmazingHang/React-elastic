import { Layout } from "antd";
const { Footer } = Layout;

const FooTer = () => (
  <Footer
    style={{
      textAlign: "center",
    }}>
    React & Django & Scrapy & Elasticsearch @2023
  </Footer>
);

export default FooTer;
